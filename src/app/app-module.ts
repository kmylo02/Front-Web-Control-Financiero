import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

// Este archivo ya no es el bootstrap principal (ver main.ts con bootstrapApplication).
// Se mantiene solo como placeholder válido para evitar errores de compilación.
@NgModule({
  imports: [BrowserModule],
})
export class AppModule {}
